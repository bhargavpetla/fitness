import { CoachHome } from "@/components/coach/CoachHome";

// AI Coach: optional 30-day meal & training plans generated from the user's
// own last 30 days, with gamified daily check-ins. Separate from manual entry.
export default function CoachPage() {
  return <CoachHome />;
}
