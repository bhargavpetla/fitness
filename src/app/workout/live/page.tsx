import { LiveWorkout } from "@/components/live/LiveWorkout";

// Live in-gym logging: pick → lift → log each set → finish → AI writes it up.
export default function LiveWorkoutPage() {
  return <LiveWorkout />;
}
