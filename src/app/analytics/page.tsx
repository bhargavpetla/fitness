import { AnalyticsHome } from "@/components/analytics/AnalyticsHome";

// WHOOP-style trends over the user's logged history: scores, trends, muscle
// focus, consistency — graphs first, words last. All computed locally.
export default function AnalyticsPage() {
  return <AnalyticsHome />;
}
